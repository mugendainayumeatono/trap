# Long-Term Memory / 长期记忆

- 项目管理工具：uv
- 代码执行和测试：始终使用 `uv` (例如：`uv run pytest`, `uv run python src/trap/main.py`)
- Project management tool: uv
- Code execution and testing: Always use `uv` (e.g., `uv run pytest`, `uv run python src/trap/main.py`)
