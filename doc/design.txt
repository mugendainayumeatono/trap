这是一个蜜罐，记录所有在监听端口上收到的数据包，应当记录数据包收到的详细时间，发送者IP，数据包内容，并尝试解码数据包内容。记录格式应当方便AI分析。提供记录到txt文件和记录到mysql数据库2种可配置的记录方案。使用环境变量设定使用哪种方案。使用环境变量指定txt文件路径或数据库服务器，账号，密码。如果是数据库，提供创建表结构的功能。如果记录到txt文件，应该提供log转储功能，由环境变量指定log到达多大时，切换到新文件。为了满足扩展需求，表结构应该可以变更。并保持对旧的数据兼容。对于所有请求，都应该返回正常，以便攻击者能进一步发送数据。并不只限制于http协议，应当支持大部分协议。应该框架化，以便可以增加针对不同协议的回复内容。尝试识别协议，在日志文件种也应当记录协议类型。应当以守护进程的方式在后台执行。用python编写，使用uv管理项目。使用docker部署，限制容器的权限，确保宿主机安全。提供compose文件，方便的启动容器。
